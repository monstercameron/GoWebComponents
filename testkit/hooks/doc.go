// Package hooks provides a first public hook-test harness for js/wasm tests.
//
// It builds on testkit/render so custom hooks and hook-driven state flows can
// be exercised without writing a bespoke host component in every test.
package hooks
