[package]
name = "bill-splitter-yew"
version = "0.0.0"
edition = "2021"

[dependencies]
yew = { version = "0.23", features = ["csr"] }
web-sys = { version = "0.3", features = ["HtmlInputElement"] }
wasm-bindgen = "0.2"

[profile.release]
opt-level = "z"
lto = true
