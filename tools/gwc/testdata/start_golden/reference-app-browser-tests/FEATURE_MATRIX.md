# Starter Feature Matrix

Preset: `reference-app` (Reference App)

This file is generated from the selected scaffold capabilities.

- [x] `router`: Route shell and navigation affordances are scaffolded in the starter layout.
- [ ] `ssr`: Server rendering and hydration expectations are documented in the generated matrix.
- [x] `forms`: Starter form interactions are scaffolded so teams can extend typed form state deliberately.
- [x] `fetch`: Async resource ownership is called out for data-loading and mutation setup.
- [x] `state`: Shared state ownership is planned as a first-class concern in this scaffold.
- [ ] `devtools`: Devtools adoption is surfaced as part of the starter capability model.
- [ ] `hot-reload`: State-preserving local reload is included as the recommended inner-loop path.
- [x] `browser-tests`: Playwright-Go smoke-test scaffolding is generated under test/playwrightgo.
- [ ] `hydration`: Client boot and hydration ownership are expected from the first app shell.
- [ ] `release-profile`: Release-minded defaults are encoded in launcher metadata and docs.
- [ ] `dev-profile`: Fast local iteration is pre-wired through gwc dev defaults.
- [ ] `browser-mount`: Direct browser mount behavior is kept explicit in the main entrypoint.
- [ ] `ui`: Component composition is scaffolded through the public ui package.
- [ ] `html`: Typed HTML builders are used as the default authoring path.

Selected capability order:

- `router`: Router
- `forms`: Forms
- `fetch`: Fetch
- `state`: State
- `browser-tests`: Browser Tests

Generated starter output is intentionally disposable; treat this scaffold as a starting point you can edit or replace freely.
