// Package pwa provides small, explicit Progressive Web App helpers that stay
// outside the core rendering runtime. The current shipped surface covers web
// app manifest generation, explicit service-worker registration and update
// lifecycle coordination, installability observation, release-manifest to
// service-worker asset planning, Cache Storage coordination, and structured
// PWA diagnostics so applications can add installability and offline behavior
// without hiding ownership inside unrelated runtime code.
package pwa
