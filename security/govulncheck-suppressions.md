# govulncheck suppressions

The root-module `govulncheck ./...` gate is blocking in the release workflow.

There are currently no accepted suppressions. If a future advisory is
temporarily accepted, record the vulnerability ID, affected package, reason,
owner, tracking issue, and removal date here before adding any CI exception.
