# v6.1 Windows verification checkpoint

This is an in-progress record, not release approval.

## Build and dependency

- Fork: `https://github.com/monstercameron/wails`
- Pushed patch: `0965e9db574e7b14a447b0ae2bf7ed36a5406462`
- Go 1.26.6, Windows amd64 EXE running on the Windows ARM64 host.
- WebView2 152.0.4191.66.
- `gwc desktop build` succeeded with the clean pinned fork.
- Actual EXE `--smoke-test` exited 0 with all 26 checks passing, including
  window round trips, low-zoom rejection without mutation, environment,
  screen geometry, URL denial, bridge contracts and two-window state isolation.

## Observed through native Windows UI

- Runtime menu replacement changed the visible menu bar.
- Native menu command delivered `tester-command` through the typed event.
- Context popup appeared; selecting its command delivered
  `tester-context-command`, menu `tester-context`, data `tester-context-scope`.
- Radio B then A delivered both checked-selection callbacks.
- Title changed visibly to `GWC Windows API Lab · portable title`.
- Single-file picker selected the synthetic `sample-a.txt`.
- Multi-file picker returned synthetic `sample-a.txt` and `sample-b.txt`.
- Folder-only picker returned the synthetic fixture directory.
- Save picker displayed the JSON filter and default filename; Escape returned
  cancellation. Its misleading “path selected” suffix was found and fixed in
  source with a passing regression test; rebuilt verification remains pending.
- Autostart: zero matching Run-key registrations before; enabling created
  exactly one expected registration; disabling restored zero. No user startup
  registration was present or modified.
- Global shortcut Ctrl+Alt+Shift+G registered, delivered
  `Shortcut event: tester-shortcut`, and was removed successfully.
- Tray fixture creation completed; taskbar interaction is not yet verified.
  The computer-use tool exposes no targetable taskbar surface; user assistance
  was requested. The test tray remains installed in the running lab meanwhile.

## Manual defects and pending verification

- Child fixture requested an unknown host template, and its error was prefixed
  “completed.” Both are fixed with passing regressions; rebuilt child lifecycle
  UI tests remain open.
- Message dialogs, remaining window controls, printing, tray selection/teardown,
  child windows, external launch/reveal, drops, and expanded screen operations
  still need the corresponding real UI checks. Unit coverage is not a substitute.
- No v6.1 root commit, release tag, or production deployment has been made.

Latest checkpoint: save-cancellation wording and child-template/error-outcome
fixes now pass focused service tests and Wasm frontend tests, but the running
EXE predates these tester fixes. The information dialog was visibly verified
and returned Ok. The warning dialog displayed its warning icon; completion
verification was interrupted by a Windows Security firewall prompt for
`gwc.test.exe` during a concurrent build-tool test run. All computer-use input
stopped at that security prompt; user dismissal is required. The tray fixture
remains present for its pending manual test; autostart and the global shortcut
were already restored before this interruption.

End of day: the maintainer requested documentation/todo updates and a stop.
DEVLOG, the unreleased CHANGELOG entry and both acceptance trackers are updated.
No further testing, commit, deployment or background continuation was started
as part of that request. The running lab/security prompt were left untouched.
