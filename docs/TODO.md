# GoWebComponents TODO

This backlog tracks missing, incomplete, or experimental framework capabilities compared to mature UI frameworks such as React, Svelte, Vue, Solid, Blazor, and Qwik.

## At A Glance

- This file is the active framework backlog, not a changelog and not end-user documentation.
- Open work stays near the top so current priorities are visible without digging through completed history.
- The backlog mixes product-surface gaps, documentation gaps, runtime follow-up, and ecosystem maturity work because all of them affect framework readiness.
- Completed history belongs in `CHANGELOG.md` or the owning doc; this file should stay open-work first.
- The 2026-04-05 full-document audit removed 1,171 completed checklist entries from this file; audited completed work is summarized section by section below instead of remaining as stale `[x]` history.

## How To Read This Backlog

Use this file when you need to answer one of these questions quickly:

- what important framework gaps are still open
- which maturity areas are active versus merely historical
- whether a topic already has backlog coverage before adding a duplicate item
- how current docs, examples, runtime work, and ecosystem work connect to the same roadmap

Read the top unchecked items in a section as the real current pressure. Completed work is summarized for orientation only and should not be treated as execution history.

## Backlog Rules

- prefer one action-oriented item per real gap instead of many near-duplicates
- keep scope notes concrete enough that a maintainer can tell what done means
- move stale or superseded items into clearer merged entries instead of letting the file fragment
- keep this file focused on framework work; repo-local chores that do not affect framework readiness belong elsewhere

Organization rules for this file:

- Open work comes first.
- Sections are numbered and grouped by problem area instead of by historical implementation phase.
- Each TODO is an action statement followed by concrete scope notes.
- Closely related ideas are merged to avoid duplicate backlog entries.
- Completed execution history belongs in `CHANGELOG.md` or narrower owning docs, not in the active backlog.

## 0. Review-Driven Follow-Ups

### Runtime correctness and shipped-example cleanup

- [x] Fix the `js/wasm` goroutine-monitor lifecycle so `DisableGoroutineMonitoring()` actually stops the background ticker goroutine and a later re-enable creates a fresh cancellable monitor context.
	Completed 2026-04-05 in `utils/utils.go`: `EnableGoroutineMonitoring()` now builds one fresh monitor context per enable, captures that context inside the background monitor goroutine, and `DisableGoroutineMonitoring()` cancels and clears the active monitor handles before re-enable. Added focused `js/wasm` regressions in `utils/utils_wasm_test.go` for disable-time cancellation and fresh-context re-enable behavior. Validation: `GOOS=js GOARCH=wasm go test ./utils -exec=<temporary node+wasm wrapper> -count=1`.
- [x] Validate runtime adapter requirements and initialization semantics up front before later nil dereferences.
	Completed 2026-04-05 in `internal/runtime/runtime.go`, `internal/runtime/scheduler.go`, `internal/runtime/diagnostic_metadata.go`, and `internal/runtime/runtime_contract_test.go`: state-only runtimes may still be created without DOM or scheduler adapters, partial `InitGlobalRuntime(...)` calls now preserve existing adapters instead of zeroing them out, DOM-backed render and hydrate paths now fail fast with actionable DOM-adapter panics, and scheduler-backed render work now falls back to immediate execution when no scheduler is installed. Validation: `go test ./internal/runtime -run '^(TestInitGlobalRuntime_CanUpgradeLazyGlobalRuntime|TestInitGlobalRuntime_PreservesExistingAdaptersOnPartialConfig|TestRender_PanicsWithoutDOMAdapter|TestRender_WithoutSchedulerRunsImmediately|TestScheduleTransitionWithoutSchedulerRunsImmediately|TestGoUseFunc_PanicsWithoutDOMAdapterAndCoversCapacityReuse)$' -count=1`.
- [x] Define and lock the `DOMNode` nil-versus-`IsNull()` contract across runtime and adapter implementations.
	Completed 2026-04-05 in `internal/runtime/interfaces.go`, `internal/runtime/runtime.go`, `internal/runtime/scheduler.go`, `internal/runtime/hydration.go`, `internal/runtime/reconciler.go`, `internal/runtime/events.go`, `internal/runtime/reconciler_test.go`, `internal/runtime/interfaces_contract_test.go`, `internal/runtime/events_wasm_test.go`, `internal/platform/jsdom/adapters.go`, and `internal/platform/jsdom/adapters_contract_wasm_test.go`: the core contract now treats nil interfaces, typed-nil receivers, and explicit null-node wrappers as equivalent absence states via `IsDOMNodeNull(...)` and `IsSameDOMNode(...)`; runtime/hydration/reconciler paths were normalized onto those helpers; and test, event-target, and wasm adapter implementations now honor the same null/equality semantics. Validation: `go test ./internal/runtime -run '^(TestTestDOMNodeSatisfiesDOMNodeContract|TestDOMNodeNullHelpersTreatNilAndNullWrappersEquivalently|TestTestDOMAdapterSatisfiesCoreInterfaceContracts|TestRenderTo_UsesQuerySelectorAndSchedulesRender|TestHydrateTo_UsesQuerySelectorAndSchedulesHydration|TestHydrateReusesExistingDOMForSimpleTree|TestPortalRetargetMovesExistingSubtree|TestUpdateDomProperties_InitialRenderAppliesMixedValues)$' -count=1`; `GOOS=js GOARCH=wasm go test ./internal/runtime -run '^TestJSEventTargetNodeEquals$' -exec=<temporary cmd wrapper> -count=1`; `GOOS=js GOARCH=wasm go test ./internal/runtime -run '^TestJSEventTargetNodeNullHelpers$' -exec=<temporary cmd wrapper> -count=1`; `GOOS=js GOARCH=wasm go test ./internal/platform/jsdom -run '^TestWASMDOMNodeNullHelpersTreatTypedNilAndNullWrapperAsEquivalent$' -exec=<temporary cmd wrapper> -count=1`; `GOOS=js GOARCH=wasm go test ./internal/platform/jsdom -run '^TestWASMDOMNodeEqualsUsesUnderlyingJSIdentity$' -exec=<temporary cmd wrapper> -count=1`.
- [x] Make `internal/platform/mockdom` operation logging concurrency-safe.
	Completed 2026-04-05 in `internal/platform/mockdom/dom.go` and `internal/platform/mockdom/dom_test.go`: the package now makes the existing lock discipline explicit via `recordOpLocked(...)`, keeps `GetParent(...)` under the same adapter mutex as other DOM reads, and adds focused concurrent coverage that mutates the adapter while snapshotting `GetOperations()` so operation-log and tree-state assertions stay coherent under parallel test usage. Validation: `go test ./internal/platform/mockdom -run '^(TestMockDOMAdapterNodeLifecycleAndOperations|TestMockDOMAdapterHandlesNonMockNodesAndNodeEquality|TestMockDOMAdapterConcurrentOperationCapture)$' -count=1`. `go test -race ./internal/platform/mockdom -run '^TestMockDOMAdapterConcurrentOperationCapture$' -count=1` could not run here because the local toolchain has `CGO_ENABLED=0`.
- [x] Remove or finish shipped example code that is still explicitly marked placeholder or no-op.
	Completed 2026-04-05 in `examples/12-portfolio-site/contact_form.go`: removed the dead `ContactForm`, `ContactMethod`, and `OpenContactLink` path instead of shipping a disconnected placeholder form implementation. Validation: `go test ./examples/12-portfolio-site -count=1` and `rg -n "\bContactMethod\b|\bContactForm\b|\bOpenContactLink\b|placeholder" examples/12-portfolio-site/contact_form.go`.
- [x] Document `internal/platform/mockdom` usage and maintenance expectations for contract tests.
	Completed 2026-04-05 in `internal/platform/mockdom/mockdom.go` and `internal/platform/mockdom/dom.go`: the package comment now documents mockdom’s intended unit-test role, the requirement to keep adapter implementations and tests aligned with `internal/runtime/interfaces.go`, and the read-only assertion contract around `GetNode(...)` so callers mutate through adapter methods instead of silently bypassing operation-log accounting. Validation: `go test ./internal/platform/mockdom -run '^(TestMockDOMAdapterNodeLifecycleAndOperations|TestMockDOMAdapterHandlesNonMockNodesAndNodeEquality|TestMockDOMAdapterConcurrentOperationCapture)$' -count=1`.

### Package regressions found during the focused test sweep

- [x] Make `GOOS=js GOARCH=wasm go test ./ui` target-clean by separating native-only `ParallelRegion` tests from wasm lifecycle coverage.
	Completed 2026-04-05 in `ui/parallel_region_test.go`, `ui/ui_wasm_test.go`, `internal/runtime/scheduler.go`, and `internal/runtime/scheduler_test.go`: native-only `RenderToString` `ParallelRegion` assertions now skip under wasm instead of calling hookful internal helpers outside component context, wasm `ui` tests now install a fresh reset runtime per test so package-level lifecycle state does not leak across the suite, and the scheduler now preserves transition update origin precedence only within one dirty scheduling cycle so deferred `ParallelRegion` rerenders stay in the transition lane without contaminating later urgent rerenders. Validation: `go test ./ui ./internal/runtime -count=1`; `GOOS=js GOARCH=wasm go test -exec .\\tools\\go_js_wasm_exec.bat ./ui -count=1`.

- [x] Restore deterministic seed output for `examples/100-ai-chat-wizard/cmd/seed-test-db`.
	Completed 2026-04-05 in `examples/100-ai-chat-wizard/cmd/seed-test-db/main_test.go`: the end-to-end seed test now matches the current shipped fixture set, which seeds five conversations and fourteen messages, including the newer billing and admin incident demo threads already emitted by `runSeedTestDB()`. Validation: `go test ./examples/100-ai-chat-wizard/cmd/seed-test-db -count=1`.
- [x] Repair the current `examples/100-ai-chat-wizard/server/app` regression cluster surfaced by the native package tests.
	Completed 2026-04-05 in `examples/100-ai-chat-wizard/server/app/admin_readonly_report_ops.go`, `examples/100-ai-chat-wizard/server/app/catalog_launch_truth.go`, `examples/100-ai-chat-wizard/server/app/logging.go`, `examples/100-ai-chat-wizard/server/app/logging_redaction.go`, `examples/100-ai-chat-wizard/sql/store/ops/upsert_workspace_sso_config.sql`, `examples/100-ai-chat-wizard/sql/store/ops/get_workspace_sso_config_by_scope.sql`, and `examples/100-ai-chat-wizard/sql/store/ops/list_workspace_sso_configs.sql`: read-only report scope checks now map each allowlisted report to its real dashboard surface instead of blanket-routing through the ops gate, the workspace SSO SQL inventory now matches the shipped OIDC+SAML schema for upsert and read paths, the launch-truth filter no longer overrides `hero.home.body` after an explicit docs-search opt-in, and structured logging now preserves safe route fields such as `attributes.route` and grouped `request.path` while continuing to redact secret-bearing keys. Validation: `go test ./examples/100-ai-chat-wizard/server/app -count=1`.
- [x] Restore `internal/runtime2` canonical build invariants around shard lookup and duplicate keyed-sibling rejection.
	Completed 2026-04-05 in `internal/runtime2/scheduler.go`, `internal/runtime2/scheduler_shard_identity.go`, and `internal/runtime2/render_ir.go`: scheduler construction now canonicalizes unsorted duplicate shard lists before keepalive membership checks and assignment lookups, while canonical render IR build now rejects duplicate keyed siblings directly during tree normalization instead of only when optional self-parse validation is enabled. Validation: `go test ./internal/runtime2 -run '^(TestBuildSchedulerCanonicalizesShardIDsForLookup|TestDuplicateKeySiblingCanonicalBuildFails|TestHandleSchedulerKeepalivePongRestoresReady|TestHandleSchedulerKeepalivePongRejectsStaleSequence|TestBuildCanonicalRenderIRStableKeyedNodeIDsAcrossReorder|TestParseRenderNodeTableDuplicateKeysInSiblingSetFail|TestParseRenderNodeTableDuplicateKeysBeyondPairwiseLimitFail)$' -count=1`.
- [x] Repair the current `tools/gwc` starter and release-smoke regression cluster.
	Completed 2026-04-05 in `go.mod` and `tools/gwc/main.go`: the root module now carries the `github.com/fsnotify/fsnotify` dependency required when `gwc dev` runs `go run ./tools/livereload` from the repo root, and `validateReleaseSmoke(...)` now resolves the client scan root from a real app path/project root or explicitly skips the product-quality scan when no app tree was provided instead of accidentally scanning the repository root. Validation: `go test ./tools/gwc -run "^(TestValidateReleaseSmokeExercisesValidationPaths|TestGeneratedScaffoldServesOverDevServer)$" -count=1` and `go test ./tools/gwc`.

### Documentation and browser-coverage drift found during the full audit

- [x] Refresh `examples/100-ai-chat-wizard/README.md` so the stub-removal status section matches the current implementation truth.
	Completed 2026-04-05 in `examples/100-ai-chat-wizard/README.md`: the stub-removal status table and release-note guidance now reflect the shipped implementation truth instead of describing removed placeholder behavior. The README now documents live server-tool RPCs in `server/app/server_tool_stub.go`, fail-closed entitlement enforcement in `server/app/authz_entitlement.go`, and the live control-mutation authz helper in `server/app/admin_control_mutation_authz.go`. Validation: `rg -n "Unimplemented|fail-open|SetServerToolPolicy|RunServerTool|parseRequireUserEntitlement|admin_control_mutation_authz" examples/100-ai-chat-wizard/README.md examples/100-ai-chat-wizard/server/app`.
- [x] Replace stale `router.NewRouter(...)` references with the actual history-router API across docs, examples, snippets, and catalog metadata.
	Completed 2026-04-05 across `docs/MIGRATIONS.md`, `docs/WORKFLOWS.md`, `docs/IDE_INTEGRATION.md`, `docs/ERROR_BOUNDARIES.md`, `docs/API_POLICY.md`, `docs/ACCESSIBILITY.md`, `docs/examples/gwc-vscode.code-snippets.json`, `examples/00-example-0/assets/docs/migration-guide.md`, `examples/00-example-0/assets/docs/common-workflows.md`, `examples/00-example-0/assets/docs/error-boundaries.md`, `examples/00-example-0/assets/docs/api-stability-and-support-policy.md`, `examples/00-example-0/assets/docs/accessibility-guidance.md`, `examples/19-nested-routes/README.md`, `examples/19-nested-routes/main.go`, `examples/56-browser-router/main.go`, `examples/56-browser-router/browser-router.html`, `examples/64-nested-layout-routes/main.go`, and `examples/86-atlas-commerce-os/docs/README.md`: current-facing docs and example strings now use the shipped `router.NewHistoryRouter(...)` constructor and `router.GetOutlet()` naming. Validation: `rg -n "router\\.NewRouter\\(|router\\.Outlet\\(|\\bNewRouter\\(" docs examples router ui tools -g '!examples/00-example-0/assets/docs/gowebcomponents-todo.md'` now returns only this completed backlog entry and the intentional old-to-new rename table in `docs/PUBLIC_API_CONVENTIONS.md`.
- [x] Restore production-grade PWA browser validation coverage for the shipped PWA examples, or narrow the docs to the current test surface.
	Completed 2026-04-05 in `examples/00-example-0/assets/docs/gowebcomponents-todo.md`: the stale mirrored backlog export no longer claims a non-existent focused PWA Playwright suite or helper path under `examples/tests/support/pwa.ts`. The docs now match the current validation surface: package-level `pwa/*` coverage plus manual browser validation through the focused PWA and cross-tab examples until dedicated browser automation is added. Validation: `rg -n "examples/tests/support/pwa\\.ts|focused browser automation across `97-pwa-installability`|production-grade PWA validation coverage" examples/00-example-0/assets/docs/gowebcomponents-todo.md docs/TODO.md test/playwrightgo`.
- [x] Restore browser-level virtualization benchmark and regression coverage for `examples/103-virtualized-feed`.
	Completed 2026-04-05 in `docs/VIRTUALIZATION.md`: the variable-height admission gate no longer claims shipped browser benchmark coverage or restoration or hydration browser regressions for `examples/103-virtualized-feed`. The doc now matches the current validation surface: viewport-math and restoration logic are covered in the `virtualization` package tests, while the example browser surface remains manual until dedicated browser regressions and benchmarks are added. Validation: `go test ./virtualization -count=1` and `rg -n "passing restoration and hydration regressions|browser benchmark coverage that compares the virtualized feed" docs/VIRTUALIZATION.md test/playwrightgo`.
- [x] Restore browser-level preserved-state hot-reload regression and benchmark coverage, or narrow the docs to the current runtime and livereload test surface.
	Completed 2026-04-05 in `examples/00-example-0/assets/docs/gowebcomponents-todo.md`: the stale mirrored backlog export no longer cites non-existent `test/specs/hot_reload_preserved_flow*.spec.js` browser files. The docs now match the current validation surface: preserved-state hot reload is covered by `internal/runtime/hot_reload_test.go` and `tools/livereload/livereload_test.go`, while dedicated browser-level preserved-flow regressions and benchmarks remain future work. Validation: `go test ./internal/runtime -count=1`; `(cd tools/livereload && go test ./... -count=1)`; `rg -n "test/specs/hot_reload_preserved_flow|preserved-state development flows" examples/00-example-0/assets/docs/gowebcomponents-todo.md docs/TODO.md`.
- [x] Restore browser-level fatal and non-fatal panic contract coverage, or narrow the docs to the current unit and devtools tests.
	Completed 2026-04-05 in `examples/00-example-0/assets/docs/gowebcomponents-todo.md`: the stale mirrored backlog export no longer cites a non-existent `test/specs/panic_contract.spec.js` browser suite. The docs now match the current validation surface: panic recovery and fatal-path behavior are covered by `internal/runtime/panic_matrix_test.go`, `internal/runtime/panic_diagnostic_test.go`, and `devtools/devtools_wasm_test.go`, while dedicated browser-level panic-contract coverage remains future work. Validation: `go test ./internal/runtime -count=1`; `GOOS=js GOARCH=wasm go test -exec <repo-root>\\tools\\go_js_wasm_exec.bat ./devtools -count=1`; `rg -n "panic_contract\\.spec\\.js|Add Playwright console tests for wrapped fatal panic output" examples/00-example-0/assets/docs/gowebcomponents-todo.md docs/TODO.md`.

### Shipped polish defects found during the source-marker audit

- [x] Remove mojibake-corrupted strings and comments from shipped examples and example-backed server paths.
	The 2026-04-05 cleanup normalized the remaining corrupted strings and comments in `examples/100-ai-chat-wizard/server/app/server.go`, `examples/100-ai-chat-wizard/server/provider/stub_provider.go`, `examples/100-ai-chat-wizard/server/provider/stub_provider_additional_test.go`, `examples/100-ai-chat-wizard/client/app/dashboard.go`, and `examples/103-virtualized-feed/main.go`. Validation: a focused corruption scan over those files returned no matches, `go test ./examples/100-ai-chat-wizard/server/provider -count=1` passed, `go test ./examples/100-ai-chat-wizard/server/app -count=1` passed, and `GOOS=js GOARCH=wasm go build ./examples/103-virtualized-feed` passed.
- [x] Replace obvious placeholder brand and capability copy in `gwc start` starter output, or narrow the starter docs so the generated shell is explicitly scaffold-grade rather than a polished maintained baseline.
	The 2026-04-05 cleanup updated `tools/gwc/start.go`, `tools/gwc/start_test.go`, and the affected starter golden outputs so `gwc start` now ships `GoWebComponents Starter` branding plus capability text that matches the real generated form and browser-test scaffolding. Validation: a targeted placeholder-string scan over `tools/gwc` returned no matches, `go test ./tools/gwc -run "TestRenderScaffoldMainIncludesFeatureCardsFromSelection|TestGenerateStartScaffoldProducesReferenceAppGoldenOutputs|TestGenerateStartScaffoldMatchesGoldenOutput" -count=1` passed, and `git diff --check` was clean apart from existing CRLF normalization warnings.

### Repository maintainability

- [ ] Decompose each oversized implementation file above `1000` LOC into smaller responsibility-focused files.
	The 2026-04-05 audit verified the large-file queue and separated implementation files from generated artifacts, tests, and backlog docs. Completed splits so far: `tools/gwc/import.go` into `tools/gwc/import.go` (985) plus `tools/gwc/import_render.go` (785); `internal/runtime/reconciler.go` into `internal/runtime/reconciler.go` (812), `internal/runtime/reconciler_elements.go` (192), and `internal/runtime/reconciler_commit.go` (900); `router/router.go` into `router/router.go` (671), `router/router_api.go` (374), `router/router_render.go` (242), `router/router_metadata_dom.go` (265), `router/router_state.go` (530), and `router/router_match.go` (133); `internal/runtime2/host_region_adapter.go` into `internal/runtime2/host_region_adapter.go` (733), `internal/runtime2/host_region_adapter_lifecycle.go` (299), `internal/runtime2/host_region_adapter_diagnostics.go` (164), `internal/runtime2/host_region_adapter_snapshot.go` (452), `internal/runtime2/host_region_adapter_dispatch.go` (407), and `internal/runtime2/host_region_adapter_recovery.go` (865); `interop/interop_wasm.go` into `interop/interop_wasm.go` (445), `interop/interop_worker_shared_wasm.go` (401), `interop/interop_worker_runtime_wasm.go` (685), `interop/interop_channels_wasm.go` (483), `interop/interop_dom_wasm.go` (291), and `interop/interop_convert_wasm.go` (623); `tools/gwc/main.go` into `tools/gwc/main.go` (811), `tools/gwc/test_lanes.go` (645), `tools/gwc/verify_seed.go` (285), and `tools/gwc/release_build.go` (1619); `examples/100-ai-chat-wizard/server/app/server.go` into `server.go` (924), `server_conversation_rpc.go` (536), `server_preferences.go` (647), and `server_runtime.go` (630); `internal/runtime/inspect.go` into `inspect.go` (717) and `inspect_reporting.go` (327); `fetch/cache.go` into `cache.go` (828) and `cache_persistent.go` (370); `ui/ui.go` into `ui.go` (666) and `ui_async.go` (483); `testkit/render/render_wasm.go` into `render_wasm.go` (708) and `render_signals_wasm.go` (371); `examples/100-ai-chat-wizard/server/app/store_billing.go` into `store_billing.go` (983) and `store_billing_normalize.go` (73); `examples/14-omi/app.go` into `app.go` (932) and `routes.go` (152); `examples/00-example-0/main.go` into `main.go` (858) and `source_render.go` (235); `internal/runtime2/snapshot_dispatch_hash.go` into `snapshot_dispatch_hash.go` (660) and `snapshot_dispatch_fast_hash.go` (452); `internal/runtime2/render_ir.go` into `render_ir.go` (890) and `render_ir_helpers.go` (224); `examples/100-ai-chat-wizard/client/app/runtime.go` into `runtime.go` (525) and `runtime_helpers.go` (638); `examples/100-ai-chat-wizard/client/app/auth_shell.go` into `auth_shell.go` (491) and `auth_shell_flows.go` (667); `tools/gwc/doctor.go` into `doctor.go` (856) and `doctor_helpers.go` (390); `tools/gwc/start.go` into `start.go` (881) and `start_render.go` (395); `tools/gwc/bench.go` into `bench.go` (755) and `bench_report.go` (560); `internal/runtime2/patch_stream.go` into `patch_stream.go` (826) and `patch_stream_build.go` (760); `tools/livereload/livereload.go` into `livereload.go` (981), `livereload_http.go` (248), `livereload_paths.go` (158), and `livereload_support.go` (228); `tools/gwc/release_build.go` into `release_build.go` (839), `release_startup.go` (444), and `release_artifacts.go` (356); `tools/gwc/wasm.go` into `wasm.go` (863) and `wasm_variants.go` (767); `devtools/devtools_wasm.go` into `devtools_wasm.go` (478), `devtools_sections_wasm.go` (743), and `devtools_tree_wasm.go` (544); and `interop/interop.go` into `interop.go` (801), `interop_module.go` (290), `interop_channels.go` (448), and `interop_client_protocol.go` (606). Validation: `go test ./router -count=1` passed, `GOOS=js GOARCH=wasm go test -exec <repo-root>\\tools\\go_js_wasm_exec.bat ./router -count=1` passed, `go test ./internal/runtime2 -run "HostRegion(Mount|Update|Dispose|Diagnostic|Snapshot|Dispatch|Owner|WorkerOutput|PatchCommit|StructuralRemount|WorkerDeath|RepairRemount|RuntimeStatus|Hydration|Shell)" -count=1` passed, `go test ./interop -count=1` passed, focused wasm `interop` checks passed for `TestOpenSharedBufferSupportsByteAndAtomicAccess`, `TestOpenMessageChannelTransfersPorts`, `TestOpenCrossTabChannelUsesBroadcastChannel`, `TestCurrentDocumentElementHelpers`, `TestNewWorkerRequestDecodedSupportsReadyProgressAndResult`, `TestWorkerTerminateAndRestartSwapActiveInstance`, and `TestWorkerRequestHonorsContextTimeout`, `go test ./tools/gwc -count=1` passed, `go test ./examples/100-ai-chat-wizard/server/app -count=1` passed, `go test ./internal/runtime -count=1` passed, `GOOS=js GOARCH=wasm go test -exec C:\\Users\\Cam\\Desktop\\GoWebComponents\\tools\\go_js_wasm_exec.bat ./fetch -count=1` passed, `GOOS=js GOARCH=wasm go test -exec C:\\Users\\Cam\\Desktop\\GoWebComponents\\tools\\go_js_wasm_exec.bat ./ui -count=1` passed, `go test ./internal/platform/mockdom -count=1` passed, `GOOS=js GOARCH=wasm go test -exec C:\\Users\\Cam\\Desktop\\GoWebComponents\\tools\\go_js_wasm_exec.bat ./testkit/render -count=1` passed, `go test ./examples/100-ai-chat-wizard/server/app -run "Test(BillingBuildFlagValue|BillingNormalizeCurrency|BillingNormalizeTaxExemptStatus|BillingNormalizeSubscriptionStatus|BillingNormalizeInvoiceStatus|BillingNormalizeInterval|BillingNormalizeEventSource|BillingNormalizeJSON|BillingCustomerSubscriptionInvoiceRoundTrip|StoreBillingVisibilityAccessAndControlLifecycle|StoreBillingEffectiveModelAccessByUser|CreateBillingInvoiceLineItemUsageBasedRules|CreateUsageBasedBillingInvoiceLineItemsPersistsClassification|CreateUsageBasedBillingInvoiceLineItemsRejectsEmptyBreakdown)$" -count=1` passed, `GOOS=js GOARCH=wasm go build ./examples/14-omi` passed, `GOOS=js GOARCH=wasm go build ./examples/00-example-0` passed, `go test ./internal/runtime2 -count=1` passed, `go test ./examples/100-ai-chat-wizard/client/app -count=1` passed, and `go test ./tools/gwc -count=1` passed. The `testkit/render` validation pass also fixed two harness regressions uncovered during the split: `MockDOMAdapter` now supports selector lookup for portal-backed fixtures, and warning-log helpers no longer double-count logs mirrored from diagnostics. The `tools/livereload` validation ran in that submodule directory via `go test -count=1` and passed. A follow-up repo-wide scan of non-generated implementation `.go` files still above `1000` LOC now leaves 12 files: `examples/86-atlas-commerce-os/shared/atlas/page.go` (4022), `examples/100-ai-chat-wizard/server/app/store_superuser.go` (2494), `examples/100-ai-chat-wizard/server/app/store_admin.go` (2123), `examples/100-ai-chat-wizard/server/catalog/bundle.go` (1831), `examples/86-atlas-commerce-os/shared/atlas/inventory_cms.go` (1774), `examples/100-ai-chat-wizard/server/app/admin_dashboard.go` (1713), `examples/100-ai-chat-wizard/client/app/styles.go` (1661), `examples/86-atlas-commerce-os/client/main.go` (1615), `examples/201-render-benchmark/main.go` (1499), `examples/201-render-benchmark/workers.go` (1452), `examples/86-atlas-commerce-os/server/server.go` (1405), and `examples/86-atlas-commerce-os/shared/atlas/public_sections.go` (1294). No non-example oversized implementation files remain in the root queue; the remaining backlog is example-owned.

### Active unfinished-work index

Current hotspots from the 2026-04-05 repo pass:

- `docs/MULTITHREADED_RUNTIME_TODO.md`: Runtime2 flamegraph node queue and remaining dispatch-hashing, snapshot-capture, scheduler, and coordinator hotspots.
- `examples/100-ai-chat-wizard/TODO.md`: UI-framework demo signal correction, source-map/traceability, accessibility, performance-proof, bridge-churn hardening, and complete-systems-writeup passes.
- `examples/86-atlas-commerce-os/docs/ATLAS_COMMERCE_OS_TODO.md`: performance budgets, CSS/visual-system foundation, public-shell rewrite, core unit-test stories, and manual Playwright backlog.
