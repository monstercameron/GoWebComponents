# GoWebComponents TODO

This page is the public documentation mirror of the framework backlog.

## Current Status

The canonical backlog is the repo-root `todos.md`. As of the current mirror
refresh, that file has no unchecked first-party backlog items.

This public page intentionally does not carry a second editable checklist. When
the root backlog changes, update `todos.md` first and then refresh this page
from that canonical state.

## Evidence

- `todos.md` contains zero unchecked `- [ ]` items.
- The completed backlog records the major framework, tooling, documentation,
  examples, testing, security, release, and agentic `gwc` work that replaced
  the older public mirror checklist.
- Current developer-facing guidance now lives in
  `docs/REFERENCE_MANUAL/README.md` and its topic chapters.
- Dense package lookup now lives in
  `docs/REFERENCE_MANUAL/16-api-browser.md`.
- Concept-to-API cross-links for the public examples site now live in
  `examples/public-examples-site/assets/docs/reference-map.md`.

## Refresh Rule

Do not add public-site-only backlog items here. This file is a mirror, not a
planning source. Add or change active framework work in `todos.md`; use this
page only to publish the canonical status.
