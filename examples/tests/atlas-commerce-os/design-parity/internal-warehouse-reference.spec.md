# design-parity/internal-warehouse-reference

Status: planning skeleton.

Reference:

- `examples/server/atlas-commerce-os/design/homepage_warehouse.tsx`

Routes:

- `/app/dashboard`
- `/app/inventory`
- `/app/warehouses/new-jersey-hub`

Assertions:

- internal header hierarchy, dashboard summary band, action cluster, dense list rhythm, and warehouse-ops hierarchy remain aligned with the reference
- parity screenshots capture start, midpoint, and end checkpoints after operator-flow functionality passes
