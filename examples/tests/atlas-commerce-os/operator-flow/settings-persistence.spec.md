# operator-flow/settings-persistence

Status: planning skeleton.

Routes:

- `/app/settings`
- `/app/settings/appearance`
- `/app/settings/locale`
- `/app/settings/workspace-defaults`

Assertions:

- theme, locale, density, and default warehouse persist after save
- saved-view import or export validation is visible
- direct-entry reload preserves the selected settings across route families
