# operator-flow/dashboard-triage

Status: planning skeleton.

Routes:

- `/app/dashboard`
- `/app/inventory/frame-desk`

Assertions:

- mock sign-in or mock session reaches the dashboard
- alert-driven navigation preserves route origin context
- threshold update refreshes dashboard summaries after returning
